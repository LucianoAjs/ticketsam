export enum PaymentStatus {
  PENDING = "pending",
  APPROVED = "approved",
  REFUSED = "refused",
}

export enum PaymentStatusPtBR {
  PENDENTE = "pendente",
  APROVADO = "aprovado",
  RECUSADO = "recusado",
}
